'use strict';

/**
 * order-confirmation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::order-confirmation.order-confirmation');
